name: Deploy Static Website to Azure Web App

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:   # Allows manual trigger from GitHub

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Detect publish profile secret
        id: detect_publish
        run: |
          if [ -n "${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}" ]; then
            echo "has_publish=true" >> $GITHUB_OUTPUT
          else
            echo "has_publish=false" >> $GITHUB_OUTPUT
          fi

      - name: Deploy using publish profile (if provided)
        if: ${{ steps.detect_publish.outputs.has_publish == 'true' }}
        uses: azure/webapps-deploy@v3
        with:
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: .                                 # Deploys everything in the root (your index.html, css, js, etc.)

      - name: Login to Azure (fallback if no publish profile)
        if: ${{ steps.detect_publish.outputs.has_publish == 'false' }}
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy using Azure login (fallback)
        if: ${{ steps.detect_publish.outputs.has_publish == 'false' }}
        uses: azure/webapps-deploy@v3
        with:
          app-name: YOUR_AZURE_WEBAPP_NAME          # ← CHANGE THIS
          package: .                                 # Deploys everything in the root (your index.html, css, js, etc.)
